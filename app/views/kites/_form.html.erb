<%= form_for(@kite, :html => {:multipart => true}) do |f| %>
  <% if @kite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kite.errors.count, "error") %> prohibited this kite from being saved:</h2>

      <ul>
      <% @kite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= @kite.user_id=current_user.id %>

  <div class="field">
    <%= f.label :Description %><br />
    <%= f.text_area :Description %>
  </div>
  <div class="field">
    <%= f.label :ImageLocation %><br />
    <%= f.text_field :ImageLocation %>
  </div>
  <div class="field">
  	<%= f.label "Local File" %><br />
  	<%= f.file_field :Upload %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
