<h1>Active Users</h1>
<table>
<% @users.each do |user| %>
  <tr>
    <td><%= link_to user.username, user %></td>
    <td><%= link_to "Add Friend", friendships_path(:friend_id => user.id), :method => :post %></td>
  </tr>
<% end %>
</table>
