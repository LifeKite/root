<%= form_for(@Invite) do |f| %>
  <% if @Invite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@Invite.errors.count, "error") %> prohibited this Invite from being saved:</h2>

      <ul>
      <% @Invite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <span>Invite a friend to use LifeKite</span>
  <div class="field">
    <%= f.text_field :email %>
  </div>
  <span>Or choose another LifeKite member</span>
  <div class="field">
    <%= f.select :user_id, User.all.map {|user| [ user.username, user.id ] }, :include_blank => true %>
  </div>
  <div class="field">
    <%= f.hidden_field "group_id", :value => params[:group] %>
  <div class="actions">
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
