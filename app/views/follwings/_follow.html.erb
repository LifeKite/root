<%= form_for(:follwing, :url => url_for(:controller => 'kites', :action => 'Follow', :type => type), :method => :put, :remote => true) do |f| %>
	<%= f.submit title, {:class => "create-button button like-button"}%>
<% end %>
